// This component is no longer used.
// The new AdminLayout will handle the layout logic.
import { ReactNode } from "react";

export function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
